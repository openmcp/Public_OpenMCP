apiVersion: apps/v1
kind: Deployment
metadata:
  name: openmcp-hpa-controller
  namespace: openmcp
spec:
  replicas: 1
  selector:
    matchLabels:
      name: openmcp-hpa-controller
  template:
    metadata:
      labels:
        name: openmcp-hpa-controller
    spec:
      serviceAccountName: openmcp-hpa-controller
      containers:
        - name: openmcp-hpa-controller
          # Replace this with the built image name
          image: atyx300/openmcp-hpa-controller:v0.0.1
          command:
          - openmcp-hpa-controller
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              #value: ""
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GRPC_SERVER
              value: "10.0.3.20"
            - name: GRPC_PORT
              value: "32061"
            - name: OPERATOR_NAME
              value: "openmcp-hpa-controller"
