apiVersion: apps/v1
kind: Deployment
metadata:
  name: openmcp-resource-collector
  namespace: rescollect
spec:
  replicas: 3
  selector:
    matchLabels:
      name: openmcp-resource-collector
  template:
    metadata:
      labels:
        name: openmcp-resource-collector
    spec:
      serviceAccountName: openmcp-resource-collector
      containers:
        - name: mcpresourcecollector
          # Replace this with the built image name
          image: atyx300/mcpresourcecollector:v0.0.4
          command:
          - mcpresourcecollector
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "openmcp-resource-collector"


