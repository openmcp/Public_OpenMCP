apiVersion: v1
kind: ConfigMap
metadata:
 namespace: metallb-system
 name: config
data:
 config: |
   address-pools:
   - name: default
     protocol: layer2
     addresses:
     #- REPLACE_ADDRESS_FROM-REPLACE_ADDRESS_TO
     - 10.0.3.191-10.0.3.200
