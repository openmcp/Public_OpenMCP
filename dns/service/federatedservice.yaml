apiVersion: types.kubefed.io/v1beta1
kind: FederatedService
metadata:
  name: kch-test-svc
  namespace: kch
spec:
  template:
    spec:
      selector:
        app: nginx
      type: LoadBalancer 
      ports:
        - name: http
          port: 80
  placement:
    clusters:
    - name: cluster2
    - name: cluster1
    - name: cluster3
