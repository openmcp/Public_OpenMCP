apiVersion: keti.example.com/v1alpha1
kind: OpenMCPDeployment
metadata:
  name: openmcp-deployment-example2
  namespace: openmcp
spec:
  replicas: 8
  labels:
      app: nginx
  template:
    spec:
      template:
        spec:
          containers:
          - image: nginx
            name: nginx
            resources:
              requests:
                memory: "1Mi"
                cpu: "100m"
