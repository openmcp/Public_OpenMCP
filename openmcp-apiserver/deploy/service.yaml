apiVersion: v1
kind: Service
metadata:
  name: openmcp-apiserver
  namespace: openmcp
spec:
  selector:
    name: openmcp-apiserver
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31635
  type: LoadBalancer
