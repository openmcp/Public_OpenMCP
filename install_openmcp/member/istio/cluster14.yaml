apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  meshConfig:
   defaultConfig:
     proxyMetadata:
       ISTIO_META_DNS_CAPTURE: "true"
  profile: remote
  values:
    global:
      meshID: mesh-openmcp
      multiCluster:
        clusterName: cluster14
      network: network-cluster14
      remotePilotAddress: 115.94.141.62
